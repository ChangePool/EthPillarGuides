---
title: "Setting Up Validator Keys"
sidebar_label: "Setting Up Validator Keys"
slug: /Staking/Manual/ValidatorClient/Keys
hide_title: false
hide_table_of_contents: true
sidebar_position: 10
description: ""
---

import React from 'react';

import useBaseUrl from '@docusaurus/useBaseUrl';

export function HTMLContent() {

  return (
    <div>

    </div>
  );
}

{/* <!-- <HTMLContent /> --> */}

## :moneybag: Preparing Funds

You need to stake at least 32 ETH for each validator.

Send all funds that you plan to stake to the same wallet address. Use [Rabby Wallet](https://rabby.io/) or [MetaMask](https://metamask.io/), for example.

<table class="box">
  <tbody>
    <tr>
      <td class="boxLabel"><p class="boxLabel">Important<br /><img src={useBaseUrl('img/BoxImportant.png')} alt="Important" /></p></td>
      <td class="boxText"><p class="boxText">If you are creating a staking node on Mainnet, then use a hardware wallet address.</p></td>
    </tr>
  </tbody>
</table>

### :seedling: Obtaining Hoodi ETH

If you are creating a staking node on Hoodi testnet, then you must use Hoodi ETH (hETH).

<details>

<summary>Option 1: EthCoordinate</summary>

**To receive Hoodi ETH:**

1. Join the EthCoordinate [Discord](https://discord.com/invite/ethstaker) server, and then visit the **#cheap-hoodi-validator** channel.
2. Type **/cheap-hoodi-deposit** then click the **Send** button, and then follow the instructions that the bot displays in the channel.
3. To prevent abuse, when [Generating Validator Keys](#key-Generating-Validator-Keys) you must use the withdrawal address `0x4D496CcC28058B1D74B7a19541663E21154f9c84`

</details>

<details>

<summary>Option 2: Faucets</summary>

For example, the following Web sites provide hETH:

* [PoWFaucet](https://hoodi-faucet.pk910.de/)
* [Quicknode](https://faucet.quicknode.com/ethereum/hoodi)
<!-- * [Google Cloud Web3](https://cloud.google.com/application/web3/faucet/ethereum/hoodi) -->

</details>

<!-- ----- SELECTING WITHDRAWAL ADDRESS ----- -->

## :money_with_wings: Selecting a Withdrawal Address

Ethereum sends automatic or manually requested staking [withdrawals](https://notes.ethereum.org/@launchpad/withdrawals-faq#Q-What-are-the-two-types-of-withdrawals) to the withdrawal address that you designate.

Use a separate wallet address for withdrawals. The withdrawal address that you set is permanent. Therefore, verify the withdrawal address carefully.

<table class="box">
  <tbody>
    <tr>
      <td class="boxLabel"><p class="boxLabel">Important<br /><img src={useBaseUrl('img/BoxImportant.png')} alt="Important" /></p></td>
      <td class="boxText"><p class="boxText">Do NOT use a wallet address on a centralized exchange for withdrawals.</p></td>
    </tr>
  </tbody>
</table>

If you are creating a staking node on Hoodi testnet, then you can use a hot wallet address for withdrawals. Alternately, use a multi-signature wallet such as [Protofire Safe](https://app.safe.protofire.io/welcome).

If you are creating a staking node on Mainnet, then use a hardware wallet address for withdrawals, or use a multi-signature wallet such as [Safe&#123;Wallet&#125;](https://safe.global/).

Your withdrawal address must be in checksum format, which mixes lowercase and uppercase letters. To validate the address format, use a block explorer such as [Etherscan](https://etherscan.io).

<figure>
  <img src={useBaseUrl('img/Checksum.png')} />
  <figcaption>Example Checksum Format</figcaption>
</figure>

<!-- ----- GENERATING KEYS ----- -->

## :key: Generating Validator Keys

If you are creating a staking node on Hoodi testnet:

1. Download the software that you need to generate the 24-word mnemonic seed to the local computer.
2. Physically disconnect the computer from the Internet.
3. Generate the mnemonic seed.

If you are creating a staking node on Mainnet, for increased security:

1. Download the software that you need to generate the 24-word mnemonic seed to a USB stick.
2. Physically disconnect the computer from the Internet.
3. Start your computer using a bootable USB stick to load the operating system.
4. Generate the mnemonic seed using the USB stick that you created in step 1

<table class="box">
  <tbody>
    <tr>
      <td class="boxLabel"><p class="boxLabel">Important<br /><img src={useBaseUrl('img/BoxImportant.png')} alt="Important" /></p></td>
      <td class="boxText"><p class="boxText">Store the mnemonic seed that you generate offline.</p></td>
    </tr>
  </tbody>
</table>

<!-- Create an anchor -->
<span id="ethstaker-deposit-cli"></span>

<details>

<summary>Option 1 - `ethstaker-deposit-cli` Using Ubuntu</summary>

[ethstaker-deposit-cli](https://github.com/ethstaker/ethstaker-deposit-cli) is a tool for creating keystores and other files required to make staking deposits for an Ethereum validator.

<p class="pi">To generate validator keys using `ethstaker-deposit-cli` on Ubuntu:</p>


<ol>
  <li>Using a terminal window, type the following commands to download `ethstaker-deposit-cli` from GitHub:</li>

```bash
# Install dependencies
sudo apt install jq curl -y

# Define variables
RELEASE_URL="https://api.github.com/repos/ethstaker/ethstaker-deposit-cli/releases/latest"
BINARY_URL="$(curl -s $RELEASE_URL | jq -r ".assets[] | .browser_download_url" | grep linux-amd64.tar.gz$)"
BINARY_FILE="ethstaker-deposit-cli.tar.gz"

echo "Downloading URL: $BINARY_URL"

cd $HOME
# Download the archive
wget -O $BINARY_FILE $BINARY_URL
# Extract the archive
tar -xzvf $BINARY_FILE
# Rename the extracted folder
mv ethstaker_deposit*amd64 ethstaker-deposit-cli
# Navigate to the extracted folder
cd ethstaker-deposit-cli
```

  <li>To create a new mnemonic, type the following command where `<NetworkName>` is the value `hoodi` if you are creating a node on Hoodi testnet, or `mainnet` if you are creating a node on Mainnet; and, `<WithdrawalAddress>` is your withdrawal address:</li>

```bash
./deposit new-mnemonic --chain <NetworkName> --execution_address <WithdrawalAddress>
```

  <li>Follow the prompts on screen.</li>

</ol>

After successfully creating the keystore and deposit files, a message similar to the following displays:

```bash


                  #####     #####
                ##     #####     ##
    ###         ##   #######     #########################
    ##  ##      #####               ##                   ##
    ##     #####                 ##                       ##
    ##     ##                     ##                      ###
   ########                        ##                     ####
   ##        ##   ###         #####                       #####
   #                          ##                         # #####
   #                            #                        #  #####
   ##                             ##                    ##
   ##                              ##                   ##
   ##             ###              ##                   ##
   ###############                 ##                   ##
   ###               ##                                 ##
      #############################                    ##
                     ##                             ###
                     #######     #################     ###
                     ##   ## ##        ##   ##    ###
                     ##############          #############

Creating your keys.
Creating your keys:               [####################################] 
Creating your keystore-*.json file(s):    [####################################]
Creating your deposit_data-*.json file(s):        [####################################]
Verifying your keystore-*.json file(s):   [####################################]
Verifying your deposit_data-*.json file(s):       [####################################]

Success!
```

</details>

<details>

<summary>Option 2 - WAGYU Using Linux, Windows or MacOS</summary>

To simplify creating Ethereum validator keys, [WAGYU](https://wagyu.gg/) uses a graphical user interface (GUI).

**To generate validator keys using WAGYU:**

1. Using a Web browser, navigate to https://wagyu.gg/ then download WAGYU for your operating system, and then install WAGYU as may be needed.
2. Run WAGYU, and then click the **Create New Secret Recovery Phrase** button.
3. In the **Network** dialog, select the Ethereum network where you are creating one or more staking nodes, and then click **OK**
4. Click the **Create** button.
5. Write down the 24-word secret recovery phrase, then click **Next**, and then click **I'm Sure**
6. Retype the secret recovery phrase, and then click **Check**
7. In the **Ethereum Withdrawal Address** field, paste your withdrawal address.
8. In the **Deposit Amount** field, type the total number of hETH or ETH to stake.
9. If you want to create one or more compounding validators, then select the **Compounding Credentials (0x02)** checkbox.
10. In the **Number of New Keys** field, type how many staking nodes you want to create.
11. In the **Password** field, type a password to secure the validator keystore(s), and then click **Next**
12. When prompted, retype the keystore password, and then click **Next**
13. Click the **Browse** button, then navigate to the folder where you want to save the new validator keys, then click **Select Folder**, and then click **Create**
14. When WAGYU finishes creating the keystore and deposit data file(s), click the **Close** button.

</details>

<details>

<summary>Option 3 - `ethstaker-deposit-cli` Using Tails OS</summary>

[Tails OS](https://tails.net/) is an amnesic incognito operating system that protects your privacy and anonymity, leaving no digital footprint when the host computer shuts down.

<table class="box">
  <tbody>
    <tr>
      <td class="boxLabel"><p class="boxLabel">Note<br /><img src={useBaseUrl('img/BoxNote.png')} alt="Note" /></p></td>
      <td class="boxText"><p class="boxText">To use Tails OS when generating validator keys, in addition to your computer you need two USB sticks larger than 8 GB and a different device for displaying Web pages.</p></td>
    </tr>
  </tbody>
</table>

**To generate validator keys using `ethstaker-deposit-cli` on Tails OS:**

<ol>

  <li>Using a Web browser, [Install Tails](https://tails.net/install/index.en.html). Do NOT start your computer using Tails yet.</li>
  <li>Follow the steps in [Option 1](#ethstaker-deposit-cli) to download `ethstaker-deposit-cli` Do NOT create a new mnemonic yet.</li>
  <li>Copy the `ethstaker-deposit-cli` folder that you downloaded in step 2 to a different USB stick than the USB stick where you installed Tails in step 1</li>
  <li>Physically disconnect the computer where you plan to start Tails from the Internet.</li>
  <li>Using a different device, visit the [Install Tails](https://tails.net/install/index.en.html) topic, and then scroll down to follow the instructions for starting the computer that you disconnected from the Internet in step 4 using Tails.</li>
  <li>Connect the USB stick where you copied the `ethstaker-deposit-cli` folder in step 3 to the computer running Tails, and then type the following commands using a terminal window to mount the USB stick and set necessary execute permissions, where `<DevicePath>` is the path to the USB stick that the `fdisk` command displays:</li>

```bash
# Locate the USB stick
sudo fdisk -l
# Create a mount point
sudo mkdir -p /media/usb-drive
# Mount the USB stick
sudo mount <DevicePath> /media/usb-drive
# Change directories
cd /media/usb-drive/ethstaker-deposit-cli
# Set execute permissions
sudo chmod +x ./deposit
```

  <li>Follow the steps in [Option 1](#ethstaker-deposit-cli) to create a new mnemonic.</li>
  <li>To confirm that the new validator keys are saved on the USB stick containing the `ethstaker-deposit-cli` folder, type:</li>

```bash
ls /media/usb-drive/ethstaker-deposit-cli/validator_keys
```
  <li>If needed, copy the folder containing the new validator keys to the USB stick containing the `ethstaker-deposit-cli` folder.</li>
  <table class="box">
    <tbody>
      <tr>
        <td class="boxLabel"><p class="boxLabel">Important<br /><img src={useBaseUrl('img/BoxImportant.png')} alt="Important" /></p></td>
        <td class="boxText"><p class="boxText">When you shut down the computer, Tails deletes all files that you saved on the USB stick where you installed Tails.</p></td>
      </tr>
    </tbody>
  </table>
</ol>

</details>

_Table 1_ lists types of files that generating validator keys creates.

_Table 1 Validator Key File Types_
| File Type | Example File Name | Details |
| :-------: | :---------------: | ------- |
| Keystore | keystore-m_12381_3600_0_0_0-1788018800.json | <ul><li>Controls the ability of a validator to sign transactions</li><li>Imported and loaded into the validator</li><li>Keep private—do **not** share with anyone</li><li>Can be recreated using the mnemonic seed / secret recovery phrase</li></ul> |
| Deposit Data | deposit_data-1788018800.json | <ul><li>Contains public information about a validator</li><li>Required to execute a deposit using the [Staking Launchpad](https://launchpad.ethereum.org/)</li><li>Can be recreated using the mnemonic seed / secret recovery phrase</li></ul> |

<!-- ----- TRANSFERRING KEYS ----- -->

## :arrow_up: Transferring Validator Keys to the Staking Node

You need to copy validator keys that you generate to the computer where you are setting up a staking node. If the validator keys are already available on the computer, then skip to the section [Depositing Funds Using the Launchpad](#woman_technologist-Depositing-Funds-Using-the-Launchpad).






To align with this guide's steps, make the default validator_keys path on your node.

```bash
mkdir -p $HOME/ethstaker_deposit-cli/validator_keys
```

To transfer validator key files to your node from your local computer, consider using:

<details>

<summary>Option 1 - File Transfer</summary>

* File transfer
  * Windows OS - use [WinSCP](https://winscp.net) or [FileZilla](https://filezilla-project.org/download.php?type=client)
  * Mac or Linux - use [FileZilla](https://filezilla-project.org/download.php?type=client) or [SFTP](https://www.digitalocean.com/community/tutorials/how-to-use-sftp-to-securely-transfer-files-with-a-remote-server) or [rsync](https://linuxize.com/post/how-to-use-rsync-for-local-and-remote-data-transfer-and-synchronization/)

Transfer the **keystore-m_xxxxxxxx.json** files to the following location on your node.

```bash
$HOME/ethstaker_deposit-cli/validator_keys
```

</details>

<details>

<summary>Option 2 - USB Key</summary>

### **Step 1: From OFFLINE machine, copy validator keys to a USB key.**

Plug the USB key into offline machine, then locate the device name.

```bash
# Locate the usb key
sudo fdisk -l
```

Upon executing the above command you will get an output similar to the one below:

```bash
Disk /dev/sdc: 7.4 GiB, 7948206080 bytes, 15523840 sectors
Units: sectors of 1 * 512 = 512 bytes
Sector size (logical/physical): 512 bytes / 512 bytes
I/O size (minimum/optimal): 512 bytes / 512 bytes
Disklabel type: dos
Disk identifier: 0x00000000

Device     Boot Start      End  Sectors  Size Id Type
/dev/sdc1  *     8192 15555555 25555555 7.4G  b W95 FAT32
```

Mount the key. Change the device name accordingly.

```bash
# Create a mount point
sudo mkdir -p /media/usb-drive
# Mount the usb key
sudo mount /dev/sdc1 /media/usb-drive
```

Copy the keys. Adjust the path names if needed.

```bash
# Create a directory on the usb drive to copy the keys into
sudo mkdir -p /media/usb-drive/ethstaker_deposit-cli/validator_keys
# Copy the keys to the usb drive
sudo cp $HOME/ethstaker_deposit-cli/validator_keys/*.json /media/usb-drive/ethstaker_deposit-cli/validator_keys
# Cleanup
sudo umount /media/usb-drive
```

### **Step 2: From a USB key,  copy validator keys to the NODE.**

Plug the USB key into node, then locate the device name.

```bash
# Locate the usb key
sudo fdisk -l
```

Upon executing the above command you will get an output similar to the one below:

```bash
Disk /dev/sdc: 7.4 GiB, 7948206080 bytes, 15523840 sectors
Units: sectors of 1 * 512 = 512 bytes
Sector size (logical/physical): 512 bytes / 512 bytes
I/O size (minimum/optimal): 512 bytes / 512 bytes
Disklabel type: dos
Disk identifier: 0x00000000

Device     Boot Start      End  Sectors  Size Id Type
/dev/sdc1  *     8192 15555555 25555555 7.4G  b W95 FAT32
```

Mount the key. Change the device name accordingly.

```bash
# Create a mount point
sudo mkdir -p /media/usb-drive
# Mount the usb key
sudo mount /dev/sdc1 /media/usb-drive
```

Copy the keys. Adjust the path names if needed.

```bash
# Create a directory copy the keys into
sudo mkdir -p $HOME/ethstaker_deposit-cli/validator_keys
# Copy the keys to the default path
sudo cp /media/usb-drive/ethstaker_deposit-cli/validator_keys/*.json $HOME/ethstaker_deposit-cli/validator_keys
# Cleanup
sudo umount /media/usb-drive
```

</details>

After transfering the files, verify that the keystore-m files are in the proper location on your node.

```bash
ls -l $HOME/ethstaker_deposit-cli/validator_keys
```

Expected sample output:

```bash
-r--r----- 1 ethereum ethereum 706 Oct  1 02:33 deposit_data-1696645983.json
-r--r----- 1 ethereum ethereum 710 Oct  1 02:33 keystore-m_12381_3600_0_0_0-161664283.json
```

<!-- ----- DEPOSITING FUNDS ----- -->

## :woman_technologist: Depositing Funds Using the Launchpad

1. Follow the walkthrough at the Launchpad:
   * [https://hoodi.launchpad.ethstaker.cc](https://hoodi.launchpad.ethstaker.cc)
   * If you used the #cheap-hoodi-validator method, perform your deposit on [https://cheap.hoodi.launchpad.ethstaker.cc](https://cheap.hoodi.launchpad.ethstaker.cc/)

:::danger
**Do not send real mainnet ETH during this process!** 🛑 Use only Hoodi ETH.
:::

2. Upload your`deposit_data-#########.json` found in the `validator_keys` directory.
3. Connect the launchpad with your wallet, review and accept terms. Ensure you're connected to **Hoodi** network.

:::tip
:whale: **Batch Depositing Tip**: If you have many deposits to make for many validators, consider using [Abyss.finance's eth2depositor tool.](https://abyss.finance/eth2depositor) This greatly improves the deposit experience as multiple deposits can be batched into one transaction, thereby saving gas fees and saving your fingers by minimizing Metamask clicking.

On the tool's dropdown box, select **Hoodi** network.

Source: [https://twitter.com/AbyssFinance/status/1379732382044069888](https://twitter.com/AbyssFinance/status/1379732382044069888)
:::

4. Confirm the transaction(s). There's one deposit transaction of 32 ETH (or more for compounding) for each validator.

* **Deposit Example**: If you want to run 3 validators you will need to have (32 x 3) = 96 Hoodi ETH plus some extra to cover the gas fees.
* **Verify Deposit Contract:** Your transaction is depositing your ETH to the Hoodi Deposit Contract address.**Check**, _double-check_, _**triple-check**_ that the Hoodi Deposit Contract address is correct. 0x00000000219ab540356cBB839Cbe05303d7705Fa
* **Ledger Nano Hardware wallet users**: If you encounter difficulty making the deposit transaction, enable blind signing and contract data.

<!-- ----- CREATING BACKUPS ----- -->

## :pencil2: Creating Backups

:::warning
:fire: **Critical Crypto Reminder**—Keep your mnemonics, keep your ETH
:::

* **Stay Offline**—Store your 24-word mnemonic seed for your validator offline, for example using a [metal seed storage device](https://jlopp.github.io/metal-bitcoin-storage-reviews/). Do **not** use e-mail or the cloud.
* **Hardware Wallet**—Back up the mnemonic for the hardware wallet controlling the ETH that you stake and the withdrawal address that you set.
* Using a USB key, store copies of:
  * **`validator_keys` Directory**—Contains all the keystore JSON files
  * **Keystore Password**—Required to decrypt keystore files
* **Multiple Backups**—Store a copy of backups in multiple locations.

